# Registre GitHub Packages pour le scope famille (auth : NODE_AUTH_TOKEN).
@mister-guiiug:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
# Bindings natifs optionnels (Vite 8 / Rolldown / oxc) — toujours installés.
include=optional
